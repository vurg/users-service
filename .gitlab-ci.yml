stages:
  - build
  - test

services:
  - docker:dind

variables:
  NEWMAN_IMAGE: postman/newman:alpine

build_job:
  tags:
    - docker
  stage: build
  script:
    - docker-compose build

test_job:
  tags:
    - docker
  stage: test
  script:
    - docker-compose up -d
